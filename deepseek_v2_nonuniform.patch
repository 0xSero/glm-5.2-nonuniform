--- /private/tmp/claude-501/-Users-sero-ai-infer/e630efd2-3725-48dd-b55c-a01187972967/scratchpad/cvllm/deepseek_v2.py.orig	2026-07-05 03:37:31
+++ /private/tmp/claude-501/-Users-sero-ai-infer/e630efd2-3725-48dd-b55c-a01187972967/scratchpad/cvllm/model_executor/models/deepseek_v2.py	2026-07-05 03:37:31
@@ -25,6 +25,7 @@
 """Inference-only DeepseekV2/DeepseekV3 model."""
 
 import typing
+import re
 from collections.abc import Callable, Iterable
 from itertools import islice
 
@@ -262,7 +263,18 @@
         self.ep_group = get_ep_group().device_group
         self.ep_rank = get_ep_group().rank_in_group
         self.ep_size = self.ep_group.size()
-        self.n_routed_experts: int = config.n_routed_experts
+        # Non-uniform REAP: per-layer routed-expert counts via
+        # config.num_routed_experts_per_layer (indexed by absolute layer idx
+        # parsed from prefix; falls back to the scalar n_routed_experts).
+        n_routed_experts: int = config.n_routed_experts
+        _per_layer = getattr(config, "num_routed_experts_per_layer", None)
+        if _per_layer is not None:
+            _m = re.search(r"\.layers\.(\d+)\.", f".{prefix}.")
+            if _m is not None:
+                _idx = int(_m.group(1))
+                if 0 <= _idx < len(_per_layer) and int(_per_layer[_idx]) > 0:
+                    n_routed_experts = int(_per_layer[_idx])
+        self.n_routed_experts: int = n_routed_experts
         self.n_shared_experts: int = config.n_shared_experts
 
         self.is_sequence_parallel = parallel_config.use_sequence_parallel_moe
@@ -275,12 +287,12 @@
 
         self.gate = GateLinear(
             config.hidden_size,
-            config.n_routed_experts,
+            self.n_routed_experts,
             prefix=f"{prefix}.gate",
         )
         if getattr(config, "topk_method", None) == "noaux_tc":
             self.gate.e_score_correction_bias = nn.Parameter(
-                torch.empty(config.n_routed_experts, dtype=torch.float32)
+                torch.empty(self.n_routed_experts, dtype=torch.float32)
             )
         else:
             self.gate.e_score_correction_bias = None
@@ -328,7 +340,7 @@
         self.experts = FusedMoE(
             shared_experts=self.shared_experts,
             gate=self.gate,
-            num_experts=config.n_routed_experts,
+            num_experts=self.n_routed_experts,
             top_k=config.num_experts_per_tok,
             hidden_size=config.hidden_size,
             intermediate_size=config.moe_intermediate_size,
